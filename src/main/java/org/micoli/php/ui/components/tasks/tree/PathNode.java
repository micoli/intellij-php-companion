package org.micoli.php.ui.components.tasks.tree;

import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;

public class PathNode extends LabeledTreeNode {
    public PathNode(@NotNull Object userObject, @Nullable String label) {
        super(userObject, label);
    }
}
